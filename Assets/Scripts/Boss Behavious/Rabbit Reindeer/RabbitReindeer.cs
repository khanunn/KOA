using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RabbitReindeer : BossBehaviourScript
{
    void LunchAttackAnimation()
    {
        BossBehaviourScript BossStat = this.GetComponent<BossBehaviourScript>();
        if(BossStat.EntityPhase == 1)
        { 

        }
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
